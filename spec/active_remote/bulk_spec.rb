require "spec_helper"

RSpec.describe ActiveRemote::Bulk do
  it "has a version number" do
    expect(ActiveRemote::Bulk::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
